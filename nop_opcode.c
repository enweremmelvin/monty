#include "monty.h"

/**
 * do_nothing - literally do nothing (bonus task I guess)
 *
 * @stack: memory address of the stack
 * @line_number: number of line being read from file
 */

void do_nothing(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
